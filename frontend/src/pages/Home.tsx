import React, { FC } from "react";
import Layout from "../components/Layout";

const Home: FC = () => {
  return (
    <Layout title="Start">
      <div>Start</div>
    </Layout>
  );
};

export default Home;

import React, { FC } from "react";
import Layout from "../components/Layout";

const About: FC = () => {
  return (
    <Layout title="Über">
      <div>Über</div>
    </Layout>
  );
};

export default About;
